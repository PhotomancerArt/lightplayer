{
  "id": "s6-corrupt-lpfs-project",
  "title": "Corrupted lpfs project data",
  "board": "C6",
  "needs": [
    "s3-current-fw-valid-project"
  ],
  "expect": [
    "sync:unreadable"
  ],
  "setup": [
    {
      "describe": "Start from s3 (firmware + project), then garble 256 KB of the lpfs partition (4 KB was NOT enough \u2014 littlefs redundancy shrugged it off; see FINDINGS.md 2026-08-03)",
      "run": "python3 -c \"import os,sys; sys.stdout.buffer.write(os.urandom(262144))\" > /tmp/lp-garbage.bin && espflash write-bin --port {port} 0x310000 /tmp/lp-garbage.bin",
      "verified": false
    }
  ],
  "manual": [
    "Open the printed Studio URL and Connect to this board.",
    "The card should surface the unreadable/recovery path, not hang on Checking.",
    "Press Enter here to finish the capture."
  ]
}
