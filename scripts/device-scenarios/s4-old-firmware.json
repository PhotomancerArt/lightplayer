{
  "id": "s4-old-firmware",
  "title": "Old firmware (wire proto mismatch / pre-hello)",
  "board": "C6",
  "needs": [],
  "expect": [
    "state:incompatible(no-hello)|state:incompatible(proto-mismatch)"
  ],
  "setup": [
    {
      "describe": "NOT AUTOMATED YET \u2014 no archived old build exists on this desk (2026-08-03 sitting). Planned automation: a FIXTURE build of the CURRENT firmware with a forced old wire proto \u2014 a cargo feature (e.g. fixture-old-proto) that sends proto = WIRE_PROTO_VERSION - 1 in the hello (and a fixture-no-hello variant that suppresses it). Same toolchain, no archived binaries, lands Incompatible(proto-mismatch)/(no-hello) deterministically. Until that exists, SKIP this scenario.",
      "run": null,
      "verified": false
    }
  ],
  "manual": [
    "Connect to this board.",
    "The card should land Incompatible with the reflash affordance."
  ],
  "port_filter": {
    "kind_contains": "USB-Serial/JTAG"
  }
}
