//! Project runtime node — the bus-scope output mirror (scoped-buses ADR).

mod project_mirror_state;
mod project_node;

pub use project_mirror_state::ProjectMirrorState;
pub use project_node::ProjectNode;
