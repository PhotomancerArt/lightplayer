# Quad strips v3 — classic ESP32 (DOM-Z-102) variant

Copy of `projects/test/quad-strips` with output endpoints renamed to the
DOM-Z-102's silkscreen labels (`ws281x:local:IO18/IO16/IO14/IO2` instead of
the XIAO S3's `D10/D9/D8/D7`). Same band colors, same shader. The desk
bring-up project for the classic bring-up roadmap's M4/G-M4 walk.
